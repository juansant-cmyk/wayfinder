[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    unit: fast tests with no database (providers, pure logic)
    integration: API tests against PostgreSQL (auth, dashboard routes)
