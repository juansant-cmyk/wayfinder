[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    unit: fast tests with no database (providers, pure logic)
    smoke: shallow page-level connectivity tests (HTTP status, minimal shape)
    integration: deep API tests — schema validation and module behavior
