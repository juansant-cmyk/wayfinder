# Simulator / emulator (same machine as backend)
EXPO_PUBLIC_API_URL=http://localhost:8000

# Physical device on Wi‑Fi — replace with your PC's IPv4 (ipconfig)
# EXPO_PUBLIC_API_URL=http://192.168.x.x:8000
