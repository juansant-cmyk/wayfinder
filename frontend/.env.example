# Simulator / emulator (same machine as backend)
# EXPO_PUBLIC_API_URL=http://localhost:8000

# Physical iPhone/Android on Wi‑Fi — use your PC's IPv4 (ipconfig → Wi‑Fi)
EXPO_PUBLIC_API_URL=http://10.0.0.34:8000

# Test account (after backend + DB are running):
#   email:    test@wayfinder.dev
#   username: testuser
#   password: wayfinder1
# Create with: python backend/scripts/seed_test_user.py
