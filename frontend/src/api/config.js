export const API_CONFIG_ERROR_MESSAGE =
  "Backend URL is missing. Set EXPO_PUBLIC_API_URL in frontend/.env";

export const API_UNAVAILABLE_MESSAGE =
  "Cannot reach the API. Check your connection, confirm EXPO_PUBLIC_API_URL in frontend/.env, and restart Expo (npx expo start --clear). If the API is on Render, the first request after idle can take up to a minute.";
